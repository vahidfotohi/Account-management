name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64

      - run: flutter pub get

      - name: Install Pods
        run: |
          cd ios
          pod install --repo-update
          cd ..

      - name: 🛠️ Hard-Fix Project File
        run: |
          cd ios
          # حذف کامل تنظیمات مربوط به Team و Signing از کل فایل پروژه
          sed -i '' '/DEVELOPMENT_TEAM/d' Runner.xcodeproj/project.pbxproj
          sed -i '' '/ProvisioningStyle/d' Runner.xcodeproj/project.pbxproj
          sed -i '' '/CODE_SIGN_STYLE/d' Runner.xcodeproj/project.pbxproj
          
          # غیرفعال کردن اجباری امضا در سطح فایل‌های کانفیگ
          echo "CODE_SIGNING_ALLOWED=NO" >> Flutter/Release.xcconfig
          echo "CODE_SIGNING_REQUIRED=NO" >> Flutter/Release.xcconfig
          echo "CODE_SIGN_IDENTITY=" >> Flutter/Release.xcconfig
          cd ..

      - name: 🚀 Build iOS (The Reliable Way)
        run: |
          # استفاده از دستور بیلد فلاتر با فلگ‌های مستقیم برای xcodebuild
          flutter build ios --release --no-codesign --extra-gen-snapshot-options="--no-precompiled" --verbose

      - name: 📦 Create IPA (Manual Packaging)
        run: |
          cd build/ios/iphoneos
          mkdir Payload
          cp -r Runner.app Payload/
          zip -qq -r FlutterIpaExport.ipa Payload/
          cd ../../../

      - name: 📤 Upload to Release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/ios/iphoneos/FlutterIpaExport.ipa
          tag: v1.0
          overwrite: true
          body: "این فایل بدون امضا (Unsigned) است و فقط روی گوشی‌های جیلبریک شده یا برای تست‌های خاص قابل استفاده است."
